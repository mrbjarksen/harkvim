local icons = require'harkvim.icons'

require'nvim-web-devicons'.setup {
  default = true,
  default_icon = icons.filesystem.file,
}

-- require'nvim-web-devicons'.set_default_icon(icons.filesystem.file)
